{
  "title": "Структура свойства - Prop",
  "text": "Свойства контейнера - находятся в объекте `props`;\r\n\r\n* `events` - события для данного свойства (если оно является событием);\r\n* `htmlLink` - html ссылка на данное свойство;\r\n* `parent` - сокращение от `parentContainer` - доступ к контейнеру из данного свойства (либо массиву если это свойство массива);\r\n* `pathToCоmponent` - имя компонента (не путать с именем контейнера т. к. они могут быть разными если компонент является массивом);\r\n* `prop` - какие либо произвольные данные, по умолчанию null (не путать с getProp и setProp );\r\n* `rootLink` - корневая ссылка на экземпляр приложения HTMLixState;\r\n* `type`- тип свойства в данном случае это \"click\";\r\n* `propName`- имя свойства в описании приложения;\r\n\r\nВ зависимости от типа свойства здесь могут добавляться и различные другие поля;"
}